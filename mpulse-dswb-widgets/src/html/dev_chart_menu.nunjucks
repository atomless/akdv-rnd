<!DOCTYPE html>
<!-- ******************************** -->
<!-- For developer convenience - generate links with query strings for loading available charts with local or live data --> 
<!-- ******************************** --> 
{{ copyright_and_versioning|safe }}
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{author_info|safe}}</title>  

    <style>
        html,
        body {
          width: 100%;
          height: 100%;
          font-size: 17px;
          line-height: 1.65rem;
          margin: 0;
          padding: 0;
          background: #ECE9E9;
        }
        * {
            box-sizing: border-box;
        }
        section {
            position: relative;
            width: 100%;
            padding: 1rem;
            margin-bottom: 3rem;
            overflow: hidden;
        }
        ul {
            width: 100%;
            padding-top: 2rem;
            padding-bottom: 2rem;
            border-top: 1px dashed lightgrey;
            border-bottom: 1px dashed lightgrey;
        }
    </style>
  </head>

  <body>
    <section>
        <h1>Run your charts with local canned json data.</h1>
        <ul class="chart-menu">
            {% for chart in charts %}
            <li><a href="{{ server_url }}{{ chart.html_filename }}.html?debug=3&use-proxy=true&data={{ chart.data_filename }}.json" target="_blank">{{ chart.name }}</a></li>
            {% endfor %}
        </ul>
    </section>
    <section>
        <h1>Run your charts with data requested from dswb-dev</h1>
        <ul class="chart-menu">
            {% for chart in charts %}
            <li><a href="{{ server_url }}{{ chart.html_filename }}.html?debug=3&dev-locale=true&use-proxy=true#tenant={{ tenant_id }}&host={{ dswb_host }}&notebookuuid={{ chart.notebook_uuid }}&widgetid={{ chart.dashboard_uuid }}" target="_blank">{{ chart.name }}</a></li>
            {% endfor %}
        </ul>
    </section>
  </body>

</html>